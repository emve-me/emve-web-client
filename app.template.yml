runtime: nodejs10
env: standard

service: website

handlers:
  - url: /
    static_files: out/index.html
    upload: out/index.html
    secure: always

  - url: /(.*\.(bmp|gif|ico|jpeg|jpg|png|css|js|ico))
    static_files: static/\1
    upload: static/(.*)
    secure: always
