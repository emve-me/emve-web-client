apiVersion: v1
kind: Service
metadata:
  name: collab-admin-svc
  annotations:
    ingress.kubernetes.io/target-proxy: https-target
spec:
  type: NodePort
  selector:
    run: collab-admin
  ports:
  - name: http
    protocol: TCP
    port: 333
    targetPort: 3035